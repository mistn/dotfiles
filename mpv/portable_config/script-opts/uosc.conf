# 进度条样式：播放时显示细线，悬停时变粗，极简且不遮挡画面
timeline_style=line

# 进度条颜色优化（纯黑/纯白灰的高对比度搭配）
color=foreground=ffffff,foreground_text=000000,background=000000,background_text=ffffff

# UI 缩放比例（如果你的屏幕是 2K/4K，可以适当调大到 1.2 或更高）
ui_scale=1.0

# 自动隐藏 UI 的延迟时间（毫秒），默认有点慢，改成 1 秒（1000）更干脆
ui_update_interval=1000